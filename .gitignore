__pycache__/

# Written by the devcontainer CLI during a build; a full build deletes it so
# that the features resolve to their newest version again (build-image.py).
image/.devcontainer/devcontainer-lock.json
